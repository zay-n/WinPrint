/**
 * React Native CLI configuration.
 *
 * The MaterialCommunityIcons.ttf font is bundled directly at:
 *   android/app/src/main/assets/fonts/MaterialCommunityIcons.ttf
 *
 * react-native-vector-icons is NOT installed as a package — the Icon
 * component in src/components/Icon.tsx renders glyphs via a plain
 * Text component using the fontFamily string, requiring no native
 * module linking whatsoever.
 */
module.exports = {};
